# LEAN & MEAN ArgoCD for Homehill
# Update: Turns out Redis is NOT optional. Who knew? 🤷
# (Ana & Bear learned this the hard way - 2026-02-01)

global:
  domain: argocd.homehill.de

redis:
  enabled: true  # ✅ Redis ist doch nötig für caching!

redis-ha:
  enabled: false  # High-availability Redis brauchen wir nicht (Homelab!)

server:
  replicas: 1
  insecure: true
  
  service:
    type: ClusterIP
    port: 80

controller:
  replicas: 1

repoServer:
  replicas: 1

dex:
  enabled: false

configs:
  cm:
    application.instanceLabelKey: argocd.argoproj.io/instance
