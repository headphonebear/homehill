# belfry — environment for docker-compose.yml
# Copy to .env and fill in. .env is gitignored; this example is committed.

# ── Postgres (reserved for the future mk3 toolkit; LMS does not use it) ──
POSTGRES_USER=mk3
POSTGRES_PASSWORD=change-me
POSTGRES_DB=mk3
